// Adding this 'fake' file so that goverge can properly report code coverage stats.
// goverge runs 'go test' on each package, which throws a 'no buildable Go source files'
// error if it doesn't find any Go sources

package goverge
